# modules/home-manager/programs/neovim.nix
# Neovim config via nixvim
{ config, lib, pkgs, ... }:

{
  # Uncomment and configure when ready to use nixvim
  # programs.nixvim = {
  #   enable = true;
  #   
  #   # Add your nixvim configuration here
  #   # See: https://nix-community.github.io/nixvim/
  # };
}
